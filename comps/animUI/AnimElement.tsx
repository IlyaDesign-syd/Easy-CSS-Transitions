import React, { useEffect, useRef, useState } from 'react';

const AnimElement = (props) => {

    return (
        <div className="animElement" style={{"backgroundColor": props.color, "width": "200px"}}>
            {/**
            TODO: Add code for animation square element 
             * 
             */}
        </div>
    )
}

export default AnimElement;